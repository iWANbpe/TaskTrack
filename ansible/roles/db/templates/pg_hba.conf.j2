local   all             postgres                                peer
local   all             all                                     peer

host    {{ db_name }}   {{ db_user }}   {{ hostvars[groups['workers'][0]]['ansible_host'] }}/32   md5

host    all             all             0.0.0.0/0               reject
