{
  "db": {
    "dbname": "{{ db_name }}",
    "user": "{{ db_user }}",
    "password": "{{ db_password }}",
    "host": "{{ hostvars[groups['db'][0]]['ansible_host'] }}",
    "port": "{{ db_port }}"
  },
  "web": {
    "host": "127.0.0.1",
    "port": {{ webapp_port }}
  }
}
